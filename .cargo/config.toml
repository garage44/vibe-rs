[target.x86_64-unknown-linux-gnu]
linker = "clang"
# lld: `pacman -S lld clang`. For faster links on large binaries, try mold instead of lld (`pacman -S mold`).
rustflags = ["-C", "link-arg=-fuse-ld=lld"]

[build]
incremental = true
